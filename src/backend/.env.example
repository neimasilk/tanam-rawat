# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Environment
ENVIRONMENT=development
DEBUG=true

# External Services
REDIS_URL=redis://localhost:6379
CLOUD_STORAGE_BUCKET=tanamrawat-images

# Monitoring
SENTRY_DSN=your-sentry-dsn-here

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tanamrawat_db

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit .env file to version control
# 4. For production, use strong random keys for JWT_SECRET_KEY

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432